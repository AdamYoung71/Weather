<Page
    Name="homepage"
    x:Class="Weather.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Weather"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent"
    >
    
    <Grid x:Name="homepageGrid">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            <RelativePanel >
                <TextBlock Name="cityNameText" FontSize="20" Foreground="White" Width="100" Height="50" Text="北京市" RelativePanel.AlignHorizontalCenterWithPanel="True"/>
                <TextBlock Name="curTempText" 
                           FontSize="80"
                           Foreground="White"
                           Width="180"
                           Text="16℃"
                           RelativePanel.AlignHorizontalCenterWith="cityNameText"/>

                <Image Name="mainIcon" Height="60"  RelativePanel.LeftOf="curTempText" Margin="0,28,10,0" Source="/Assets/Icons/white/0@2x.png"/>
                <TextBlock Name="tomoText" Foreground="White" RelativePanel.AlignBottomWith="cityNameText" Text="tomorrow" FontSize='20'/>
            </RelativePanel>
        </StackPanel>
    </Grid>
</Page>
